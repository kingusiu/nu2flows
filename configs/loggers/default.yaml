wandb:
  _target_: pytorch_lightning.loggers.wandb.WandbLogger
  entity: kinga-wozniak
  offline: False
  id: null
  log_model: False
  project: ${project_name}
  name: ${network_name}
  save_dir: ${paths.full_path}